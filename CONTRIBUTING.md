# Contributing to Cradle

Use `pnpm install`, then run `pnpm check-types`, `pnpm lint`, and `pnpm test` before opening a pull request. Keep public contracts in `@cradle/core` backward compatible or document a migration.

Never add private crawl data, API keys, or production visitor transcripts to issues, tests, or fixtures.
